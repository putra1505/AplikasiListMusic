<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AplikasiSubmission" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gray_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/Emerald</item>
        <item name="colorOnSecondary">@color/white</item>
    </style>
    <style name="Theme.AplikasiSubmission" parent="Base.Theme.AplikasiSubmission" />
</resources>